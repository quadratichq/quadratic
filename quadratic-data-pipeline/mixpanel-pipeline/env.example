# Meltano uses environment variables in the format: {PLUGIN_TYPE}_{PLUGIN_NAME}_{SETTING}
# For example: TAP_MIXPANEL_API_SECRET, TARGET_S3_PARQUET_S3_BUCKET

# Mixpanel Configuration (tap-mixpanel)
TAP_MIXPANEL_API_SECRET=your_mixpanel_api_secret_here
TAP_MIXPANEL_PROJECT_ID=your_mixpanel_project_id_here
TAP_MIXPANEL_START_DATE=2024-01-01
TAP_MIXPANEL_PROJECT_TIMEZONE=UTC
TAP_MIXPANEL_DATE_WINDOW_SIZE=30
TAP_MIXPANEL_ATTRIBUTION_WINDOW=5
TAP_MIXPANEL_USER_AGENT=tap-mixpanel/1.0

# Parquet Configuration (target-parquet)
# Writes Parquet files directly using Meltano
TARGET_PARQUET_DESTINATION_PATH=/project/output/
TARGET_PARQUET_COMPRESSION=snappy

# Standard AWS environment variables (for boto3/awswrangler)
# For LocalStack - use dummy credentials
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1

# LocalStack configuration (running at project root)
AWS_ENDPOINT_URL=http://host.docker.internal:4566
AWS_S3_ENDPOINT_URL=http://host.docker.internal:4566
AWS_GLUE_ENDPOINT_URL=http://host.docker.internal:4566
AWS_ATHENA_ENDPOINT_URL=http://host.docker.internal:4566

# Meltano Configuration
MELTANO_ENVIRONMENT=dev
MELTANO_DATABASE_URI=sqlite:///meltano.db