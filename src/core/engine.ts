import CanvasInput from "../components/CanvasInput";

const EngineEngine = function (width: number, height: number) {
  let ci = new CanvasInput();
  return [[ci], []];
};

export default EngineEngine;
