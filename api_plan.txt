new_empty()
new_from_file(file_contents)
get_render_cells(rect) -> borders, formatting, and values
set_cell_value(rect, value?)
delete(rect)

get_code_cell_value(pos) -> CodeCellData
set_code_cell_value(pos, code_cell)
run_code_cell(pos)

undo()
redo()


sheet, x, y, w, h


// clears if null
get_formatting_summary(rect) -> is anything bold? anything italic? (yes/no/some)
set_cell_borders(rect, [[data]]?)
set_cell_bold/italics/etc(rect, data?)

copy(rect)
cut(rect)
paste(pos)



move_cells(rect, pos)

add_column_before(column)
add_row_before(row)

add_column_after(column)
add_row_after(row)

remove_column(column)
remove_row(row)

move_column(column, after_what)
move_row(row, after_what)



Transaction = Vec<Command>

enum Command {
    SetCellDependencies {
        cell: CellRef,
        dependencies: Vec<CellRef>
    },
    SetCellValues {

    }
}
