//! Draws lines in text (eg, strikethrough)

import type { DrawRects } from '@/app/shared/types/size';
import { Graphics } from 'pixi.js';

export class CellsDrawRects extends Graphics {
  update(drawRects: DrawRects[]) {}
}
