version: 1
frontend:
  phases:
    build:
      commands:
        - ./scripts/client-ci-vercel-build.sh
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - quadratic-client/node_modules/**/*
      - quadratic-core/target/**/*
