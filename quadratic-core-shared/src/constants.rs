//! Shared constants for renderer and core.

/// Width of a cell hash bucket (number of columns per hash)
pub const CELL_SHEET_WIDTH: u32 = 15;

/// Height of a cell hash bucket (number of rows per hash)
pub const CELL_SHEET_HEIGHT: u32 = 30;
