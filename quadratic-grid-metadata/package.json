{
  "name": "quadratic-grid-metadata",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --out-dir ../quadratic-client/src/quadratic-grid-metadata --weak-refs",
    "dev": "cargo watch -s 'wasm-pack build --dev --target web --out-dir ../quadratic-client/src/quadratic-grid-metadata --weak-refs'"
  }
}
